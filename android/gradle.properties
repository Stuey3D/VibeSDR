# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx6144m -XX:MaxMetaspaceSize=2048m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# ARM only — every real Android phone is ARM; x86/x86_64 are emulators/rare
# Chromebooks and would just bloat the build with the bundled SDR++ native core
# in V4. (Override per-build with -PreactNativeArchitectures=… if ever needed.)
reactNativeArchitectures=armeabi-v7a,arm64-v8a

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
edgeToEdgeEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
# ★★★ MUST BE TRUE, AND IT IS cloudflared THAT REQUIRES IT. With `false`, native libraries are
# never extracted: they stay inside the APK and are mapped from there. VERIFIED ON THE MOTO
# 2026-08-22 — the app's lib/arm64 directory was EMPTY on an install built this way.
# ★★ cloudflared is not a library, it is an EXECUTABLE we run as a child process, and Android
#    (API 29+, W^X) refuses to exec anything from writable app storage. Shipping it as
#    libcloudflared.so in nativeLibraryDir is the only place it can be both present and runnable —
#    and that only happens when the libraries are actually extracted to disk.
# ★ The cost is a whole-app packaging change: libraries are compressed in the APK and unpacked at
#   install, so the download shrinks and the installed footprint grows.
expo.useLegacyPackaging=true

expo.inlineModules.watchedDirectories=[]