#!/bin/sh
# ★ (2026-09-22) The armhf package now carries an ARMv6 scalar build as well, and postinst installs
#   it on a CPU without NEON — so nothing is refused here any more. Kept as an empty hook so a later
#   release has somewhere to put a pre-install check without changing the package layout.
set -e
exit 0
